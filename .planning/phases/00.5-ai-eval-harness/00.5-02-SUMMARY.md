---
phase: 00.5-ai-eval-harness
plan: "02"
subsystem: testing
tags: [longmemeval, eval, benchmark, retrieval, tdd]

# Dependency graph
requires:
  - phase: 00.5-ai-eval-harness-01
    provides: eval/metrics.py with MetricsResult dataclass and metric functions
provides:
  - LongMemEvalAdapter class with run_question, run_fixture, aggregate methods
  - LongMemEvalResult dataclass for per-question scored results
  - LONGMEMEVAL_FIXTURE: 10-question subset covering all 5 LongMemEval categories
affects:
  - Phase 7 (Hybrid RRF) — wire LongMemEvalAdapter to core.retrieval.RetrievalEngine.active_search()

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD: RED (failing import) → GREEN (full implementation) with atomic commits per phase"
    - "Retrieval callable interface: retrieval_fn(query str) -> list[str] for deferred wiring"
    - "Case-insensitive substring match for answer scoring in benchmark adapter"
    - "LONGMEMEVAL_DATASET_PATH env var pattern for switching between fixture and full dataset"

key-files:
  created:
    - eval/longmemeval_adapter.py
    - eval/longmemeval_test.py
  modified: []

key-decisions:
  - "Case-insensitive substring match chosen for answer scoring — matches LongMemEval's loose evaluation protocol"
  - "LONGMEMEVAL_DATASET_PATH full-dataset loading deferred to Phase 7 via NotImplementedError with clear message"
  - "Category lookup uses _FIXTURE_CATEGORY_MAP dict keyed by question_id — O(1) per result, no fixture re-scan"

patterns-established:
  - "Retrieval callable interface pattern: retrieval_fn(query str) -> list[str] — all eval adapters use this signature"
  - "Phase-deferred wiring comment: TODO(Phase N): Wire to core.X.method() once Phase N is complete"

requirements-completed: [EVAL-01]

# Metrics
duration: 2min
completed: 2026-03-29
---

# Phase 00.5 Plan 02: LongMemEval Adapter Summary

**LongMemEvalAdapter with 10-question fixture covering all 5 benchmark categories, scoring via case-insensitive substring match, runnable offline without network or core imports**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-29T19:39:11Z
- **Completed:** 2026-03-29T19:41:10Z
- **Tasks:** 1 (TDD: 2 commits — test then feat)
- **Files modified:** 2

## Accomplishments

- LongMemEvalResult dataclass with question_id, expected_answer, retrieved_context, is_correct, score fields
- LONGMEMEVAL_FIXTURE with 10 realistic questions across all 5 LongMemEval taxonomy categories (2 per category)
- LongMemEvalAdapter.run_question() scoring via case-insensitive substring match against retrieval callable output
- LongMemEvalAdapter.aggregate() returning overall accuracy and per-category breakdown
- 14 tests covering correct/wrong scoring, case-insensitivity, empty retrieval, fixture completeness, aggregate math
- Adapter documented for Phase 7 wiring to core.retrieval.RetrievalEngine.active_search()

## Task Commits

Each task was committed atomically (TDD: test commit then feat commit):

1. **Task 1 RED: Failing tests** - `95737bd` (test)
2. **Task 1 GREEN: LongMemEval adapter implementation** - `5af9d82` (feat)

**Plan metadata:** (docs commit — see below)

_Note: TDD task has two commits (test RED then feat GREEN)_

## Files Created/Modified

- `eval/longmemeval_adapter.py` - LongMemEvalAdapter class, LongMemEvalResult dataclass, LONGMEMEVAL_FIXTURE (10 questions, 5 categories)
- `eval/longmemeval_test.py` - 14 tests covering scoring, fixture completeness, aggregate accuracy, importability guard

## Decisions Made

- Case-insensitive substring match for answer scoring: matches LongMemEval's loose evaluation protocol where partial matches count
- LONGMEMEVAL_DATASET_PATH full-dataset path raises NotImplementedError with clear Phase 7 note rather than silently ignoring
- Category lookup via pre-built `_FIXTURE_CATEGORY_MAP` dict avoids rescanning fixture on each aggregate call

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

Pre-existing issue discovered (out of scope): `eval/staleness_test.py::test_single_memory_update_v1_to_v2` fails with `TypeError: NoneType is not callable` because it calls `MemoryStore(...)` directly without checking `_CORE_STORAGE_AVAILABLE`. This is a pre-existing defect unrelated to this plan. Logged to deferred-items.md.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- LongMemEvalAdapter is ready to accept any retrieval callable matching `(query: str) -> list[str]`
- Wire to `core.retrieval.RetrievalEngine.active_search()` in Phase 7 (Hybrid RRF)
- Full 500-question benchmark runnable once LONGMEMEVAL_DATASET_PATH is set and Phase 7 loading is implemented

---
*Phase: 00.5-ai-eval-harness*
*Completed: 2026-03-29*
