---
name: memory
description: deprecated — use /stats (memory counts), /health (archival candidates), /threads (narrative threads), or /usage (injection and usage statistics) instead
---

# Memory — DEPRECATED

This skill has been retired and split into four focused, purpose-built skills:

- **`/stats`** — View memory counts by stage, importance distribution, and cross-project view
- **`/health`** — Check relevance health: archival candidates, rehydration candidates, stale memories
- **`/threads`** — Browse and explore narrative threads
- **`/usage`** — Track injection counts, usage patterns, and importance trends

Each skill has a sharp trigger description and optimized output for its specific purpose.

---

For help with any of these skills, use `/memesis:stats`, `/memesis:health`, `/memesis:threads`, or `/memesis:usage` directly.
